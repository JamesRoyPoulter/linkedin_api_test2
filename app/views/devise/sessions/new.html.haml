/ %h2 Sign in
/ = form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
/   %div
/     = f.label :email
/     %br/
/     = f.email_field :email
/   %div
/     = f.label :password
/     %br/
/     = f.password_field :password
/   - if devise_mapping.rememberable?
/     %div
/       = f.check_box :remember_me
/       = f.label :remember_me
/   %div= f.submit "Sign in"

%h3.margin_button Click and please let it load, we are pulling your daytah

%div.btn.btn-primary.btn-larg.margin_button=link_to "Login with LinkedIn", user_omniauth_authorize_path(:linkedin), :id => "linkedin_button"

%br
%br

= render "devise/shared/links"